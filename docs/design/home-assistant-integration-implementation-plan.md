# Implementation plan: OpenNVR × Home Assistant

| | |
|---|---|
| **Design** | [home-assistant-integration.md](home-assistant-integration.md) (r3) |
| **Status** | In progress (see [Progress](#9-progress)) |
| **Revision** | r3 (2026-09-18): execution revisions from code, HA 2026.9 API and environment research, plus a self-review. r2: first issue breakdown |
| **Audience** | Whoever implements or reviews this work |

## 1. How to read this
- Work is split into **milestones (M0–M6)** of **issues (HA-xxx)**, in execution order.
- **Sizes:** S ≤ 1 working session, M = 2–3 sessions, L = 4+ sessions.
- **Code homes:**
  - **core** = this repo.
  - **lib** = `integrations/home-assistant/pyopennvr/`.
  - **ha** = `integrations/home-assistant/hass-opennvr/`.
  - Both `integrations/` folders mirror their future repo roots and are split out at 1.0 (HA-306).
- Line numbers were taken on 2026-09-18 (main `9fc355e`). Re-check them before editing.

## 2. Engineering rules (every issue)
- **Branches and commits:**
  - One issue, one `SRB-ha-<id>-<slug>` branch, stacked on the previous issue's branch, squashed to **one commit** (`feat(ha): <title> (HA-xxx)`). No Co-Authored-By trailer.
  - Branches stay local and are pushed **per milestone**, after:
    - a `git rebase --update-refs origin/main` of the whole stack (backup tags first);
    - a full retest;
    - a code and security review pass;
    - the owner's go-ahead.
- **Schema changes:**
  - Every change goes in `server/models.py` **and** an Alembic migration in `server/migrations/versions/` (`<12hex>_<desc>.py`). Head at the start was `b7e4a1c9d302`; HA-002 added `c4d8e2f1a9b3`.
  - New columns are **nullable or have a `server_default`**, because `init_db()` runs `create_all` and stamps fresh databases, so migrations never run there (`server/core/database.py:289-320`).
  - Every migration implements `downgrade()`. `server/tests/test_migration_graph.py` stays green.
- **Permissions:**
  - New permissions are seeded in `scripts/init_db.py` and backfilled on upgrade with the `main.py:216-258` pattern.
  - **Grants must preserve behaviour.** No existing user may gain or lose an ability as a side effect.
- **Server tests:** per-file in-memory SQLite with `StaticPool` and `app.dependency_overrides` (e.g. `server/tests/test_alerts_inbox.py`). Results are compared with the recorded host baseline (§8).
- **Integration and lib tests:** run in a Linux `python:3.14` container (`scripts/ha-dev/test-integration.ps1`). HA 2026.9 needs Python ≥ 3.14.2 and doesn't support Windows.
- **Audit:** every state-changing endpoint calls `services/audit_service.write_audit_log`. The actor and correlation id come from the request context (HA-002).
- **HA contract:** changes to the REST fields HA uses, websocket v2 messages or descriptors follow the §6.11 rules of the design, and bump `CONTRACT_VERSION`.
- **Live verification:**
  1. Build **one reusable tag**, `ghcr.io/open-nvr/core:ha-dev`.
  2. Recreate only core, with the stack's own `-f`/`--profile` set: `$env:CORE_TAG='ha-dev'; docker compose … up -d --no-deps opennvr-core`.
     - **Never** edit `.env`.
     - **Never** use `--remove-orphans`: it deletes the fakecams and apps-profile containers.
  3. Restore `core:main` at the end of each session.
  4. Read core's logs from the supervisord files inside the container, and audit rows through the audit-logs API.
- **Security traps to avoid** (found in review):
  - **Never mutate the ORM `User` on behalf of a token.** Tokens resolve to a read-only `TokenPrincipal` proxy (HA-101).
  - **Never `ContextVar.set()` inside a sync dependency.** It runs in a copied threadpool context; mutate the shared `RequestContext` object instead (HA-002).
  - **`is_active=false` stops recording.** Tokens need the `recording_pause_enabled` flag to change it (HA-106/108).

## 3. Execution revisions (r3): research findings that shaped this plan
| # | Finding | Consequence |
|---|---|---|
| R1 | HA 2026.9 needs Python ≥ 3.14.2. `pytest-homeassistant-custom-component==0.13.365` pins HA 2026.9.2. There are no Windows HA tests. | Integration and lib tests run in a Linux py3.14 container. |
| R2 | The most widely used existing NVR integration predates current HA practice (`hass.data`, no reauth or repairs, ICE ignored). | Nothing is copied. The integration is written against HA 2026.9's developer docs, and existing integrations are only a reference for how the APIs behave and which problems to avoid. |
| R3 | HA signs relative, query-free media URLs itself (`async_sign_path`, 24 h), and the signature covers one exact path. | The media browser uses `requires_auth` proxy views with query-free paths. HLS segments are validated by prefix against the manifest signature. |
| R4 | Native HA WebRTC: `async_handle_async_webrtc_offer`, `async_on_webrtc_candidate`, `close_webrtc_session`. | WHEP: POST the offer, rewrite `Location` to `/webrtc/…` (as `app/src/lib/streamUrl.ts` does), PATCH candidates, DELETE on close. |
| R5 | Camera scoping lives in 6+ unshared helpers. | A central gate in the token resolver, plus fixes to each helper and a test per helper. |
| R6 | Bespoke auth in `recordings._authenticate_request` and `apps._service_or_user_principal`. | Both gain the `onvr_` branch. |
| R7 | `get_current_superuser` guards 117 routes; there are 51 inline `is_superuser` checks. | Tokens never pass superuser routes; the `TokenPrincipal` reports `is_superuser=False`. |
| R8 | Named permissions are enforced on only about 6 routes. | `RequirePermission` is added to every route HA uses. |
| R9 | `write_audit_log` commits internally; there is no request context. | New `core/request_context.py`: one contextvar holding a mutable object. |
| R10 | Detect-pipeline already honours roster `analyze`, and KAI-C dispatch runs in that worker. | The detection flag is a column plus a roster field. |
| R11 | The tier0 consumer returns early when the overlay is off and drops non-drawable tracks. | live-state hooks in before those filters. |
| R12 | PTZ move/stop use `onvif_digest_service`; presets exist only in IP-keyed onvif-zeep code. | New digest preset functions that reuse the `PTZService` config cache. |
| R13 | Browser websockets can't send `X-Device-Token`. | The firewall decision is bound into the websocket ticket at mint time. |
| R14 | There is no shared `SecuritySetting` helper. | New `services/site_settings.py`. |
| R15 | `create_all` plus stamping on fresh databases. | Rules in §2. |
| R16 | The e2e suite is on the local-only branch `test/e2e-suite` (210 behind main); `tests/e2e/.artifacts` holds credentials. | HA-005 rebases the branch; `.artifacts` is gitignored (done in HA-000). |
| R17 | C: has 22 GB free (Docker's disk lives there); D: has 48 GB. | One reusable image tag; HA dev config on D:. |
| R18 | `hass-web-proxy-lib` exists. | We write our own small aiohttp streaming proxy (fewer dependencies, core-eligible). |
| R19 | Since HA 2026.3 an integration can ship a local `brand/` folder. | `hacs.json` sets `homeassistant: 2026.3.0` and ships `brand/icon.png`. |

## 4. Decisions
| # | Decision | Status |
|---|---|---|
| D0 | Code home and delivery | **Locked:** monorepo until 1.0; stacked local branches, pushed per milestone |
| D1 | `pyopennvr` owner and licence | Default: open-nvr org, Apache-2.0 (matches the SDK). Confirm before HA-306. |
| D2 | Site mode scope | Default: v1 = arming affects alert delivery only |
| D3 | Can non-admin tokens create manual events? | Default: only with an explicit `events.create` grant |
| D4 | Retention of hashed evidence exports | Default: protected until an admin unflags them |
| D5 | Descriptor governance | Default: the core team owns `platform` and the fields; app descriptors must pass `opennvr-app validate` |
| D6 | mDNS on Docker Desktop | Default: unsupported; manual URL entry |
| D7 | Recording pause | **Locked:** behind the site flag `recording_pause_enabled`, off by default |

## 5. Milestones
| Milestone | Outcome | Exit criteria |
|---|---|---|
| **M0 Prerequisites** | Existing gaps that block HA work are closed | ffmpeg in core; correlation ids and audit coverage; permissions seeded; e2e suite on main; websocket firewall; segment-playback spike answered |
| **M1 Core foundations** | Everything HA needs exists in `/api/v1` and websocket v2 | Contract 1.0.0 frozen; fixtures published; server suite and e2e smoke green |
| **M2 Integration 0.1** | Dev integration: cameras live, core entities, Repairs | e2e: setup → entities → occupancy → HA restart restores state |
| **M3 Integration 1.0** | Services, media browser, notifications, card session, Gold rules | Gold checklist green; notification relay verified on mobile data; repo split and PyPI publish (with the owner) |
| **M4 MQTT discovery** | Zero-install path | A clean HA with only MQTT discovers the devices |
| **M5 Assist** | LLM tools | An Assist query answered within the token's cameras |
| **M6 Spikes** | ONVIF server and Matter bridge go/no-go | A spike report for each |

## 6. Issues (execution order)

### M0: Prerequisites (core)
| ID | Issue | Edit map (key files and reuse) | Tests / acceptance | Size |
|---|---|---|---|---|
| HA-000 | Execution setup | This r3; design r3; the tracker; `integrations/home-assistant/` scaffold; `scripts/ha-dev/`; `.gitignore` for e2e artifacts; baseline test run | Scripts run; baseline recorded (§8) | S |
| HA-001 | ffmpeg in the core image | Root `Dockerfile` runtime stage (apt list, lines 81-91) | `GET /recordings/frame` returns a JPEG on a built image | S |
| HA-002 | Request context and correlation ids | New `core/request_context.py` (contextvar holding a mutable `RequestContext`); `middleware/request_logging.py:67-169` (validate inbound `X-Correlation-Id`, else the request id; set before the quiet-path return; response header); `AuditLog.correlation_id` plus migration; `write_audit_log` defaults from the context and merges the actor into `details.actor`; CORS allow/expose (`main.py:756-763`) | Inbound, invalid and generated ids; audit row carries it; actor mutation visible from both sync and async routes | M |
| HA-003 | Audit gaps | The `cameras._record_audit_log` pattern (`cameras.py:190`) at: PTZ move/stop (2235, 2284); `alerts_inbox.acknowledge` (305); `recordings.create_export_ticket` (881) and `set_recording_flag` (821); `routers/device_firewall.py` (91-136); `routers/skills.py` declare/release (133, 163) | One audit assertion per endpoint | M |
| HA-004 | Permissions | `scripts/init_db.py` seeds `camera_device.write`, `ptz.control`, `events.create`, `apps.actions`, `recordings.pause`, `api_tokens.manage`. **Grants:** `ptz.control` to every role holding `live.view`; nothing else beyond admin/full_access. Upgrade tuple in `main.py:216-258`; `backfill_permission(db, source, target)` generalised from `services/apps_view_backfill.py` | Fresh and upgrade DB; a viewer can still PTZ; an operator gains nothing | S |
| HA-005 | Land the e2e suite | Rebase `test/e2e-suite` onto main (backup tag); resolve conflicts | `tests/e2e/run.py -m smoke` green (needs the dev stack stopped: container names collide) | M |
| HA-006 | Websocket device firewall | `_mint_ws_ticket` (`events.py:111`) stores the firewall decision and IP; handler (202-341) re-checks before `accept()` using `enforcement_active_cached`, `get_client_ip`, `is_loopback`, `is_internal_service` | Enforcement on and unapproved → close 1008 | S |
| HA-007 | Spike: in-progress segment playback | Read-only probe of MediaMTX `/playback/list` and `/get` for `now-20s…now-5s` | Result recorded in the tracker; sets the HA-113 clip timing | S |

### M1: Core foundations
| ID | Issue | Edit map | Tests / acceptance | Size |
|---|---|---|---|---|
| HA-104 | `/system/info` + `services/site_settings.py` | `routers/system.py`; `site_id` UUID in `SecuritySetting`; `contract_version`, `features[]` (incl. `recording_pause_enabled`), opt-in `UPDATE_CHECK`; model on `apps._registry_info` (`apps.py:1004`) | Stable `site_id`; flag reflected | S |
| HA-105 | Health and camera stats | `SystemMonitorService.sample()` (`system_monitor_service.py:104-150`): volumes, memory, GPU (null); `days_retained`; `GET /cameras/{id}/stats`: 10 s `bytesReceived` sampler via `MediaMtxAdminService.get_active_path_info`; new reducers in `services/tier0_metrics.reduce_metrics` for latency and skipped per camera; `_derive_recording_state` | Reducer unit tests on sample metrics; endpoint with MediaMTX mocked | M |
| HA-101 | API tokens | `ApiToken` plus migration; `services/api_tokens.py` (the `app_keys` pattern: SHA-256, `compare_digest`, `onvr_`); `routers/api_tokens.py` (`api_tokens.manage`; tokens can't mint tokens). `core/auth.get_current_user(request, …)` returns a **read-only `TokenPrincipal`** (never mutates the User). It mutates the request-context actor, checks `allowed_cidrs`, and throttles `last_used`. **Central camera gate:** `camera_id`, `cam_id`, `camera_ids`, `cam-N` / `camN` handles; body endpoints check explicitly. `get_current_superuser` refuses tokens; `user_has_permission` / `RequirePermission` require the scope. Also fix: `camera_scope.visible/manageable_camera_ids`, `CameraService.get_camera_by_id` / `user_has_permission`, `recordings._can_view_camera` / `_viewable_cameras`, `streams._check_camera_permission`, the `GET /cameras` list, `recordings._authenticate_request`, `apps._service_or_user_principal`; the device firewall passes tokens. Grep for ORM-instance uses of `current_user`. | Matrix: token × every scoping helper; revoked/expired; CIDR; superuser refused; audit actor; admin row never modified | L |
| HA-102 | Token UI | `app/src/views/Settings.tsx` registry, `views/settings/ApiTokens.tsx`, react-query, en/fr locales | `npm run build`; manual SPA pass | M |
| HA-103 | Websocket ticket for tokens | `events.py:137` mint carries the token and its allow-list; `_ws_scope_for` (190) intersects | Token websocket sees only its cameras | S |
| HA-106 | Detection flag | `Camera.detection_enabled` plus migration; `CameraUpdate` / `CameraResponse`; `update_camera` pops `reason` before the setattr loop (like `assignments` at 1111) and audits it; roster `list_camera_agent_sources` (~822-834) emits `analyze` | Flag off → no tier0 for that camera (checked on the live `tracks` ws) | S |
| HA-107 | PTZ presets, manual events, protect | `onvif_digest_service`: `ptz_get/goto/set_preset_digest` (next to 687/719); `PTZService.presets`; routes `/cameras/{id}/ptz/presets[...]` with `ptz.control` + audit, and `ptz.control` on move/stop **in addition to** the existing ownership check (never replacing it); `timeline_service.record_manual_event` / end; `routers/timeline_events.py` `POST /events` (`events.create`), `PUT /events/{id}/end`, `POST /events/{id}/protect` (helper factored out of `set_recording_flag`) | Preset goto audited; manual event listed; protect flags the segments | M |
| HA-108 | Recording pause (flagged) + camera on/off for tokens | `POST /cameras/{id}/recording`: site flag first, then `recordings.pause`; reuses the body of `toggle_camera_recording` (2141-2206); persisted auto-resume via `spawn_background`; audit; rule comment at 2133 updated; `PUT /system/settings/recording-pause` (superuser). **Tokens changing `is_active` also need the flag.** | Flag off → 403; on → pause, resume, audit; token `is_active` blocked without the flag | M |
| HA-109 | Core zones | `CameraZone` plus migration and CRUD; zone editor reusing `GeometryEditor` (props 110-131); `TimelineEvent.zone_ids`; detect-pipeline `events_poster` sends an optional `best_box` → `TrackEventIn` → `record_track_visit` computes zones | CRUD; the event row has zones; detect-pipeline tests | L |
| HA-110 | live-state | `services/live_state.py` (pure logic): per camera/zone/label total and active (`not stationary`) counts, `LIVE_STATE_STALE_S`, track start/end, motion debounce, last plate/face, rate-limited last-object crop (`capture_frame_bytes` → `save_evidence_jpeg`). Hooked into `tier0_track_consumer._handle_message` before `to_overlay_payload`; the overlay flag gates only overlay publishing. `GET /live-state` | Synthetic tier0 frames: counts, staleness, start/end, zones; live fakecam counts | L |
| HA-111 | Websocket v2 | `event_bus_service`: `seq`, 5-min ring buffer, `event_type` filters, new publishers; `events.py`: `v=2`, `since`, `state_snapshot` first; extended alert publish (`alerts_inbox._handle_message` 352-360) | v1 shape pinned; resume/replay/snapshot; `scripts/ha-dev/ws_load.py` with 20 subscribers | L |
| HA-112 | Signed media | `services/media_signing.py` (HMAC, `kid`, rotation in site_settings); `POST /media/sign`, `GET /media/s/{token}`; sources `evidence_store.resolve_evidence`, event evidence, and `_stream_playback_clip` factored out of `recordings.export_clip` (956-1007); audited fetch; `release(db)` before streaming | Expiry, tamper, wrong kind → 404; rotation revokes | M |
| HA-113 | `media_ready` | Alerts when stored; events after `record_track_visit`; clip timing from HA-007 | Alert → `media_ready` → signed clip plays | M |
| HA-118 | Site mode v1 | `/site-mode` (`settings.view` / `settings.manage`), site_settings; gate in `alarm_actions.dispatch_alarm_actions` (201-204), bypassed on `force`; websocket `site_mode`; audit | Arming changes delivery | M |
| HA-114 | Entity descriptors | `services/entity_descriptors.py`: core descriptors (the recording switch only when the flag is on); app descriptors from `manifest_json["entities"]`; server-side state resolution (live_state, stats, a cached 5 s poll of the app's `/state` reusing the `get_app_status` fetch, a Python dot-path evaluator); `entity_state` + `GET /entities` (ETag); typed commands `POST /entities/{key}/command` (`core_control` enum, `app_action` via `invoke_app_action`, widened to tokens with `apps.actions` + audit); SDK `Entity` dataclass, `AppManifest.entities`, `to_dict`, `validate.check_manifest`, facade; version bump (**check `publish-sdk.yml` for auto-publish first**); `descriptors_changed`; retrofit `examples/abandoned-object` | Descriptors listed and states pushed; forbidden commands rejected; SDK tests | L |
| HA-115 | Contract | `server/contract/` JSON schemas; `CONTRACT_VERSION = "1.0.0"`; `scripts/contract_check.py` in CI | CI blocks a breaking diff | M |
| HA-116 | Search API | `/search`, structured, over events (incl. `zone_ids`) and alerts; camera-scoped | Filters and scoping | S |
| HA-117 | LAN exposure | Docs for `RTSPS_BIND_HOST`, `MEDIAMTX_WEBRTC_HOSTS`, `CORS_ORIGINS`; `scripts/mdns-announcer/` (host network, profile `mdns`, Linux-only) | TXT-builder unit tests; **unverified end to end on Docker Desktop** | M |

### M2: `pyopennvr` and integration 0.1
| ID | Issue | Contents | Size |
|---|---|---|---|
| HA-201 | `pyopennvr` | aiohttp client (injected session); `OpenNVRError` / `OpenNVRAuthError` / `OpenNVRConnectionError`; typed models; `EventStream` (ticket, v2, `since`, snapshot, backoff, tolerant reader); WHEP helper; contract-fixture tests | L |
| HA-202 | Skeleton | `ConfigEntry[OpenNVRData]` runtime_data; coordinator with `config_entry=` (push plus 30 s refresh); config flow (user, zeroconf, reauth, reconfigure, options); `async_set_unique_id(site_id)`; diagnostics; base entity; manifest (no MQTT); `hacs.json`; `brand/`; `quality_scale.yaml`; test helpers on phcc fixtures | M |
| HA-203 | Camera | WebRTC via WHEP (answer, candidate PATCH, DELETE, `WebRTCError`); client config with ICE; still image; detection toggle; `ON_OFF` **only when the flag is on**; RTSPS `stream_source` | L |
| HA-204 | Descriptor platforms | sensor, binary_sensor, switch, select, button, number, event, image; dynamic add/remove; stale-device cleanup; unknown platforms skipped | L |
| HA-205 | Update + alarm panel | — | S |
| HA-206 | Repairs | The ids in design §7.9; fix flows | M |
| HA-207 | E2E `ha-dev` | Automated onboarding and config flow; entities; occupancy; restart restore; revoke → Repair; audit correlation | M |

### M3: Integration 1.0
| ID | Issue | Contents | Size |
|---|---|---|---|
| HA-301 | Services | `services.yaml`, `SupportsResponse` | M |
| HA-302 | Media browser | Own identifiers; `requires_auth` proxy views; HLS prefix validation; own streaming proxy | L |
| HA-303 | Notification relay + blueprint | `/api/opennvr/{site}/m/{token}` (signed tokens only); blueprint | M |
| HA-304 | Card session | `opennvr/card_session`; allow-listed passthrough; CORS; ACC engine patch for the owner to submit | L |
| HA-305 | Gold readiness | `quality_scale.yaml`; translations (incl. exceptions and icons); docs; CI (py3.14 pytest + hassfest at `integrations/home-assistant/hass-opennvr`) | M |
| HA-306 | Split and release | `git subtree split` for both folders; the owner creates the repos, publishes `pyopennvr` to PyPI and tags the release; HACS action on the new repo | M |

### M4: MQTT discovery (core)
| ID | Issue | Contents |
|---|---|---|
| HA-401 | Real MQTT integration type | `integration_service.py`, `Integrations.tsx`, connection test (`aiomqtt`) |
| HA-402 | Discovery from descriptors | Device-based payloads; LWT; re-publish on HA online |
| HA-403 | Commands and events | `…/set` → typed commands (token-bound principal); CloudEvents on the event topic |
| HA-404 | Deprecate `home-assistant-relay` | ROADMAP, COMPARISONS, catalog note |

### M5: Assist
| ID | Issue | Contents |
|---|---|---|
| HA-501 | `llm.py` | `llm.API` registered once globally, five tools |
| HA-502 | Semantic search | Inspect and reuse the `footage-search` app first; `/search` stays the one contract |

### M6: Spikes
- **HA-601 ONVIF server** (Profile S + M PullPoint prototype).
- **HA-602 Matter bridge** (the first question is whether bridged cameras are allowed at all).

Each spike ends with `docs/design/spikes/<name>.md`.

## 7. Risks tracked during implementation
| Risk | Owner issue | Mitigation |
|---|---|---|
| In-progress segment not playable | HA-007 / HA-113 | Wait for the segment to close; send the image first and the clip later |
| WHEP trickle quirks | HA-203 | Non-trickle fallback (gather all candidates first) |
| Token gate misses a camera path | HA-101 | Per-helper test matrix; grep of camera params |
| Admin demoted through ORM mutation | HA-101 | `TokenPrincipal`; ORM-use grep; a test asserts the admin row is unchanged |
| e2e rebase conflicts | HA-005 | Resolve toward main; cherry-pick harness and smoke only if it balloons |
| HA API drift | M2+ | Pinned to 2026.9.2; bumping is its own issue |
| C: disk | all | One image tag; prune dangling layers per milestone; HA config on D: |

## 8. Test baseline (recorded in HA-000)
Recorded 2026-09-18 on the Windows dev host, base `58c59e1` (main `9fc355e` + design docs). Later runs must show **no failures beyond these**.

| Suite | Command | Result | Known host-only failures |
|---|---|---|---|
| server | `server\.venv\Scripts\python -m pytest tests -q` (py3.14) | 1519 passed, 7 skipped, **4 failed** | `test_m1b_mediamtx_hardening::test_mediamtx_external_rtsps_url_defaults_to_none`, `…::test_browser_urls_fall_through_to_internal_when_externals_unset` (dirty `server/.env`), `…::test_cert_script_is_idempotent` (no WSL bash), `test_m1c_transport_probe::test_probe_supported_against_real_tls_listener` (temp-file lock) |
| sdk | `uv run --frozen --group dev pytest -q` in `sdk/opennvr-app-sdk` | 636 passed, 1 skipped, **4 failed** | `test_credentials::test_site_key_until_an_app_key_is_issued` (POSIX file mode), `test_docs_site::test_no_page_is_orphaned_from_the_nav` (backslash paths), `test_frame_sources::test_factory_routes_file_scheme` (Windows `file://` parsing), `test_scaffold_and_config::test_scaffolded_app_smoke_test_passes` (WinError 10106 in a subprocess) |
| pyopennvr | `scripts/ha-dev/test-integration.ps1 -Suite lib` | 1 passed | — |
| hass-opennvr | `scripts/ha-dev/test-integration.ps1 -Suite ha` + `scripts/ha-dev/hassfest.ps1` | 1 passed; hassfest 0 invalid, 0 warnings | — |

All eight host failures come from the Windows host, not the code; they pass on Linux CI.

## 9. Progress
| ID | Status | Branch | Commit | Notes |
|---|---|---|---|---|
| HA-000 | done | SRB-ha-000-execution-setup | (this commit) | Scaffold + dev scripts verified: both suites pass in the py3.14 container; hassfest clean. Baselines in §8. The `test_*.py` gitignore needed a negation for `integrations/home-assistant/**/tests`. PowerShell 5.1 scripts use `$ErrorActionPreference='Continue'` plus `$LASTEXITCODE`, because docker writes progress to stderr. |
| HA-001 | done | SRB-ha-001-core-ffmpeg | (this commit) | Before: `core:main` `/recordings/frame` → 502 "Could not extract frame". After, on `core:ha-dev`: 200 image/jpeg for cams 1 and 3 (ffmpeg 7.1.5). The image grows ~330 MB (1.57 → 1.9 GB; Debian ffmpeg pulls codec libs), more than the ~100 MB estimated. Added `scripts/ha-dev/swap-core.ps1` (recreates core only, carries `OPENNVR_HOST_IP`/`OPENNVR_LAN_IPS`, never edits `.env`) and `mint-jwt.ps1`. |
| HA-002 | done | SRB-ha-002-correlation-ids | (this commit) | `core/request_context.py` (one contextvar holding a mutable object); middleware sets it before the quiet-path return and echoes `X-Correlation-Id`; `audit_logs.correlation_id` via migration `c4d8e2f1a9b3`. **The real head was `b7e4a1c9d302`, not `a3f19c7d2e60` as §2 assumed.** The audit API returns and filters `correlation_id`. Live: the migration applied on core:ha-dev and a logout with `X-Correlation-Id` produced an audit row carrying it. Server suite: 1534 passed, baseline 4 failed. **Carry-over:** the audit API field is checked live with HA-003's image; the audit UI doesn't show the column yet (later polish). |
| HA-003 | done | SRB-ha-003-audit-gaps | (this commit) | New `audit_service.audit_request` helper: it never fails the action being audited, and records the real client IP via `core.client_ip.get_client_ip` (trusted-proxy XFF only). Audited: `ptz.move`/`ptz.stop`, `alerts.ack` (only when something was silenced), `recording.protect`/`unprotect`, `recording.export`, `device_firewall.enforcement/approve/block/delete`, `skill.claim`/`release`. Live on core:ha-dev: a test alarm, ack and export ticket under one correlation id gave 2 audit rows carrying it, and the audit API returns and filters `correlation_id` (closes the HA-002 carry-over). Server suite 1545 passed, baseline 4 failed. |
| HA-004 | done | SRB-ha-004-permissions | (this commit) | New `services/permission_catalog.py` is the single list for both seed paths; the upgrade logic moved out of `main.py` into testable `seed_new_permissions(db)` (apps.install/apps.view rules unchanged). `backfill_permission(source, target)` generalised from `apps_view_backfill` (thin wrapper kept). Defaults: `ptz.control` to operator and viewer; nothing else beyond admin. Live upgrade on this install: all 6 seeded once; `ptz.control` granted to the 3 roles holding `live.view` (admin, operator, viewer); no other grants. Server suite 1551 passed, baseline 4 failed. **Rule for HA-107 (review correction):** PTZ today requires camera **ownership** or superuser (`CameraService.get_camera_by_id`), and the IP-keyed ONVIF tools require the `manage` tier. It is NOT "anyone who can watch". `ptz.control` must be enforced **in addition to** those checks, never instead of them, or every `live.view` holder would gain PTZ on shared cameras. The permission and its grants are created in one transaction. |
| HA-005 | done | SRB-ha-005-e2e-suite | (this commit) | The 8 `test/e2e-suite` commits are squashed into this one (backup tags `backup/test-e2e-suite-pre-ha005`, `backup/SRB-ha-005-e2e-suite-pre-squash`); the one `.gitignore` conflict is resolved. **Correction:** the suite runs ALONGSIDE the dev stack (containers `opennvr_e2e_*`, +20000 ports, own subnet); no downtime was needed. Fix added: `run.py` test-binds every published host port before `compose up` and moves an unbindable one (Docker's backend held 20080) to the next free port. Result against core:ha-dev: **47 passed, 3 skipped** (the first-time-setup tests need `--fresh`), 0 failed. Run: `$env:CORE_TAG='ha-dev'; server\.venv\Scripts\python.exe tests/e2e/run.py` (the detect-pipeline `ha-dev` tag is a local alias of `:main`). |
| HA-006 | done | SRB-ha-006-ws-device-firewall | (this commit) | Firewall facts (client IP, device token, internal-key) are bound to the ws ticket at mint and re-checked at the handshake (`_ws_firewall_allows`), mirroring the HTTP middleware order. While enforcing, the handshake must come from the minting IP (a leaked ticket can't borrow an approval). Kept in a parallel map, so the ticket store's shape is unchanged. Live (enforcement **left off**, deliberately, so the owner's browser isn't locked out): ticket → subscribed → tier0 tracks flowing; replay refused. Refusal paths are covered by unit tests. Server suite 1563 passed, baseline 4 failed. |
| HA-007 | done | SRB-ha-007-playback-spike | (this commit) | **Answer: yes, the in-progress segment is playable.** MediaMTX 1.15.4 `/playback/get` returned 200 video/mp4 for windows starting 20, 12 and 6 s ago on cam-1 and cam-3 (the current segment is listed while still growing). **HA-113 decision:** a clip's `media_ready` fires at event end + 2 s; no need to wait for segment close. Clips may start at the preceding keyframe, which is fine. Probe kept as `scripts/ha-dev/probe_playback.py` (read-only, runs inside core). |
| HA-008 | done | SRB-ha-008-client-ip-spoofing | (this commit) | **Pre-existing critical bug found by the M0 review.** nginx appends to `X-Forwarded-For`, and `get_client_ip` took the LEFT-most (client-written) entry, so `X-Forwarded-For: 127.0.0.1` resolved to loopback. That bypassed the device firewall, defeated the HA-006 ws IP binding, and let clients pick the IP stored by HA-003 audit rows. Now the header is walked from the right, skipping trusted hops (right-most hop if all are trusted; `X-Real-IP` fallback). The new tests fail 7/14 on the old code. Live: the same forged request was recorded as `127.0.0.1` before, `172.28.0.1` (the hop nginx saw) after. Server suite 1583 passed, baseline 4 failed. |
| M0 review | done | (folded into HA-002/003/004/006) | — | Independent review, 10 findings: #1 → HA-008; #2 PTZ premise corrected (ownership gates PTZ; `ptz.control` only ever narrows); #3 ONVIF-tools PTZ audited; #4 permission + grants in one transaction; #5 self-heal creates `index=True` indexes; #6 service ws ticket only from inside the stack; #7 end-to-end ws test; #8 audits write through their own session; #9–10 log correlation id, hint-not-attribution docs. |
| M0 exit | done | pushed as `SRB-ha-integration` | 41eaa2b | Final e2e 47 passed / 3 skipped. The owner chose **one PR for the whole implementation, one commit per issue**, so all further issues are committed directly on `SRB-ha-integration` and pushed at each milestone end. |
| HA-104 | done | SRB-ha-integration | (this commit) | `services/site_settings.py` (shared key/JSON helper: site id created once and race-safe, site name, `recording_pause_enabled` true only if exactly `true`); `core/contract.py` (`CONTRACT_VERSION` 0.1.0 until HA-115 freezes 1.0.0, `FEATURES`); `GET /system/info`; opt-in `UPDATE_CHECK` (off by default, 6 h cache, failure → null). Live: site_id stable across a core restart. Server suite 1592 passed, baseline 4 failed. |
| HA-105 | done | SRB-ha-integration | (this commit) | `GET /cameras/{id}/stats` (`services/camera_stats.py`) joins MediaMTX path info (ready; bitrate by differencing `bytesReceived` between reads: the first read is null, and a counter reset or a gap over 300 s is null), tier0 metrics (fps, target fps, mean `inference_ms`, `skipped_total`, tracks, frame age; the new `_sum_by_camera` sums across the model/reason labels that `_by_camera` silently dropped) and the DB (recording state via `_derive_recording_state`, `days_retained`). Absent sources are null, never errors. `/system/resources` gains `gpu: null`. Live: cam1 bitrate 1050 kbit/s on the 2nd read, inference 731 ms (CPU box). Note: `/system/resources` is empty for ~15 s after a core restart, until the first monitor sample (pre-existing). Server suite 1597 passed, baseline 4 failed. |
| HA-101 | done | SRB-ha-integration | (this commit) | `ApiToken` table + migration `d7f3a1b9c2e4` (downgrade drops it). Tokens are `onvr_<8hex prefix>_<secret>`, only the SHA-256 stored, secret shown once. `services/api_tokens.py`: deny-by-default `TOKEN_ROUTES` (method + full route template → required scope; `/events/ws-ticket` waits for HA-103); read-only `TokenPrincipal` (`is_superuser` always False, setattr raises, the owner row is never touched); a permission needs the scope AND the owner still holding it; central camera gate over `camera_id`/`cam_id`/`camera_ids`/`camera=camN`/`path=cam-N` (unparseable → 403); `allowed_cidrs`; `last_used` throttled to 1/min in its own session. `camera_scope`, `GET /cameras/`, `recordings._can_view_camera`/`_authenticate_request`, `apps._service_or_user_principal` and the device firewall are token-aware. Other per-route helpers (`streams`, `CameraService`) are covered by the gate plus the route allow-list. `/api-tokens` CRUD needs `api_tokens.manage`, can't grant more than the creator holds, and refuses tokens. Grep for ORM-instance uses of `current_user`: none. **Trap found live:** the core image resolves FastAPI 0.141.1 (the lock pins 0.135.3; the image's `uv sync --frozen` falls back to an unlocked sync), where `scope["route"].path` loses the include prefix, so every token got 403. `_route_key` now rebuilds the full template on both versions, with a test through real router nesting; the token tests pass on both 0.135.3 and 0.141.1. Live via nginx: allowed 200, other camera 403, list narrowed to [1], `/users/` and `/audit-logs/` 403, `/api-tokens` 403, revoke → 401, audit `details.actor = token:<name>` with the caller's correlation id. Server suite 1638 passed, baseline 4 failed. |
| HA-102 | done | SRB-ha-integration | (this commit) | Settings > **API Tokens** tab (`views/settings/ApiTokens.tsx`), following the Device Firewall panel's pattern (plain `apiService` + state; no settings panel uses react-query). Create form: name, scopes (preset = what HA needs: cameras/live/recordings/alerts/settings view; scopes the user lacks are shown disabled), all cameras or a picked list, optional allowed networks, expiry. The secret is shown once with a Copy button (execCommand fallback for plain-HTTP LANs) and is cleared when that token is revoked; reloads after create don't unmount it. Table: prefix, scopes, cameras/networks, last used + IP, expiry, status, Revoke (confirm). Hidden behind `api_tokens.manage`. en + fr strings. e2e: `tests/ui/test_api_tokens_page.py` creates a token on the page, uses the shown secret against the API (200 on `/system/info`, 403 on `/api-tokens`), revokes it on the page and gets 401; a second test checks the secret is gone after a reload. `tsc --noEmit` and `npm run build` clean; e2e UI 4/4, harness 15/15. |
| HA-103 | done | SRB-ha-integration | (this commit) | `POST /events/ws-ticket` joins `TOKEN_ROUTES` (needs `cameras.view`). A token ticket carries `api_token_id` in its `WsTicketBinding` and answers `kind: "token"`. At the handshake `api_tokens.principal_for_ws` re-checks the token (revoked/expired inside the 30 s ticket life, owner disabled, `allowed_cidrs` against the socket's own address, scope) and the socket runs as the `TokenPrincipal`, never the owner. Camera scope = `visible_camera_ids` (owner ∩ allow-list, already token-aware). New **event-type entitlement** on the bus (`_Subscriber.allowed_event_types`, a boundary like `allowed_camera_ids`): a token receives only the types in `TOKEN_EVENT_SCOPES` its scopes cover (camera_status→cameras.view, tracks/inference_*→live.view, camera_event→recordings.view, app_alert→alerts.view); unlisted types never reach a token. The `subscribed` frame lists them for tokens only (user sockets byte-identical). Device firewall: a token ticket passes without an approved browser, from the minting IP only. Mutation-checked (dropping the handshake re-check or the CIDR check fails the tests). Live via nginx: cam1 token socket received only `tracks` for cam 1; `camera_id=2` refused; after revoke the ticket mint is 401. Server suite 1646 passed, baseline 4 failed. |
| HA-106 | done | SRB-ha-integration | (this commit) | `Camera.detection_enabled` (nullable, NULL = on so existing cameras keep detecting) + migration `e2a9c4f7b1d5` (downgrade drops it). `CameraUpdate.detection_enabled` and an audit-only `reason` (popped before the setattr loop, never a column); `CameraResponse.detection_enabled` (NULL → true). The camera-agent roster sends `analyze` (R10 verified: detect-pipeline `_to_spec` honours it and reconcile drops the worker). App rosters get the field too; apps' own inference is still governed by their assignments. Audit row carries `reason` and `detection_enabled: {from, to}`. **Tokens:** `PUT /cameras/{id}` joins `TOKEN_ROUTES` (`cameras.manage`), and a token may change only `TOKEN_CAMERA_FIELDS` = {detection_enabled} (name, source, credentials and `is_active` → 403; `is_active` comes in HA-108 behind the flag). `get_camera_or_403` now judges a token by its OWNER's rights narrowed to the allow-list (it compared the token principal's `is_superuser`, always False, so an admin's token could not reach cameras another user created). SPA: "Object detection" checkbox in the camera edit form and a "Detection off" badge in the list, so a switch flipped from HA is visible and reversible (en/fr). Live: token toggled cam 1 off → tracks stopped within ~35 s; on → back after ~75 s (worker warm-up); token rename → 403. Server suite 1651 passed, baseline 4 failed. |
| HA-107 | done | SRB-ha-integration | (this commit) | **PTZ presets:** `onvif_digest_service.ptz_get/goto/set_preset_digest` (preset XML parsed across prefix/attribute-order dialects; preset tokens validated against `^[A-Za-z0-9_.:-]{1,64}$` BEFORE any SOAP is built, names XML-escaped), `PTZService.presets/goto_preset/set_preset` on a shared `_run` (cached config, one re-resolve; a 422 is not retried). Routes `GET/POST /cameras/{id}/ptz/presets`, `POST …/presets/{token}/goto`, audited (`ptz.preset_save/goto`). **`ptz.control` ADDED to move/stop** on top of the ownership check (HA-004 pre-granted it to every `live.view` role). **Manual events:** `POST /events` (`events.create`; body camera checked explicitly for tokens and for existence — a superuser could otherwise file an event on a nonexistent camera; label `[a-z0-9 _.-]`, ≤24 h backdate, no future, optional duration), `PUT /events/{id}/end` (manual only, once: 409 otherwise), `source=event_type="manual"`, payload `note` + `created_by` (the token/user actor). **Protect:** `POST /events/{id}/protect` flags clips overlapping [start−pre_s, (end or now)+post_s] via the new `services/recording_protection.set_range_protection`, which `PUT /recordings/flag` now shares. Behaviour change there: **overlap instead of "starts inside"** — the old rule left the segment holding an incident's first seconds unprotected. All six routes in `TOKEN_ROUTES`. Live via nginx with a cam-1 token: open event 201 (`created_by: token:…`), cam 2 → 403, end 200 then 409, protect flagged 2 clips (released again after the check), presets on a fakecam → 400 (no ONVIF creds; no PTZ camera on this rig, so preset SOAP is covered by unit tests only). Server suite 1667 passed, baseline 4 failed. |
| HA-108 | done | SRB-ha-integration | (this commit) | `POST /cameras/{id}/recording` `{enabled, resume_after_s?, reason?}`. Pausing: **site flag first** (403 "not allowed on this site", before MediaMTX is touched), then `recordings.pause`, then camera (ownership; tokens: allow-list; a turned-off camera → 409). Resuming is always allowed (`recordings.pause` or `cameras.manage`). New `services/recording_pause.py`: MediaMTX `record` off + `CameraConfig.recording_enabled=False` (the startup re-provisioner honours it, so a pause is durable) and a `recording_paused` site setting {since, resume_at, by, reason}. Auto-resume timer; `restore_on_startup` (hooked after boot provisioning) reschedules, resumes overdue ones, and resumes everything if the flag is off. System resumes are audited (`recording.resume`, actor `system:auto-resume`). The timer unregisters itself before resuming (resume() cancels the registered task, which would otherwise be itself; test fails without the fix). `GET/PUT /system/settings/recording-pause` (superuser; off → resumes every paused camera, audited). Tokens: `PUT /cameras/{id}` may change `is_active` only while the flag is on (turning a camera off stops recording). `/cameras/{id}/stats` gains `recording_paused`. SPA: Settings > Recording "Pausing recording" card (superuser): the opt-in with a confirm, and the paused cameras with Resume buttons (en/fr). The old unrouted toggle's comment now points here. Live via nginx: flag off → 403; token paused cam 1 with a 150 s auto-resume → MediaMTX `record` false, state "off"; **core restarted mid-pause** → still paused; at +179 s recording back on, pause cleared, audited. Flag left off. Server suite 1677 passed, baseline 4 failed. |
| HA-109 | done | SRB-ha-integration | (this commit) | `CameraZone` (camera_id, name unique per camera, normalised polygon 3..32 points, optional label filter) + `TimelineEvent.zone_ids` (JSON; NULL = not computed, [] = no zone), migration `f4b8d2a6c9e1` (downgrade drops both). `routers/zones.py`: `GET/POST/PUT/DELETE /cameras/{id}/zones` (read = camera visibility; write = `cameras.manage` + camera; ≤32 zones/camera; audited). Tokens: GET only. **Pipeline:** `VisitLifecycle.observe(..., frame_size)` records the bottom-centre of the box (where the object stands) per frame, thinned to ≤32 points spread over the whole visit (halve-and-double-step), posted as optional `path`. Core `services/zones` tags the visit at ingest (even-odd point-in-polygon, label filter); a bad or missing path never costs the visit. `GET /events` returns `zone_ids`. SPA: Camera-Config > **Zones** (camera picker, zone list, draw on the snapshot with the app GeometryEditor, other zones shown as references; en/fr). Test-harness fix: the shared token `env` fixture now imports the core modules it shares at collection time and overrides every reachable `get_db` (conftest's per-module purge of `core.*` otherwise left later modules with a second request-context/`get_db`, sending requests to the real database). Live (detect-pipeline `ha-dev` build, env identical to the running one, restored to `main` afterwards): a new person visit on cam 1 came back `zone_ids=[1]` (whole-frame zone, not the corner one). Server suite 1694 passed (baseline 4 failed); detect-pipeline 452 passed. |
| HA-110 | done | SRB-ha-integration | (this commit) | `services/live_state.py` (pure, clock-injected): per camera the tracks present now (overlay's `track_is_drawable` + 0.25 floor), counts per label as `{total, active}` (active = not stationary) for the whole picture and per zone (bottom-centre of the box; zone label filters), track start/end, motion debounce (on at once, off after `LIVE_STATE_MOTION_OFF_S`=10), staleness (`LIVE_STATE_STALE_S`=5). **Finding:** Tier-0 publishes only frames WITH tracks (`publish_empty=False`), so an emptied scene goes quiet rather than sending an empty frame. `stale` therefore means "nothing seen lately", and a 1 s sweeper task (`run_live_state_sweeper`, in main) ends quiet cameras' tracks and publishes the drop to zero. Hook in `tier0_track_consumer._handle_message` BEFORE the overlay filters. `DETECTION_OVERLAY_ENABLED=false` still stops the consumer entirely (unchanged: no track data reaches any consumer), so there are no live counts then (corrected in the M1 review). New bus event `live_state` (state + started + ended), sent on change only; token scope `cameras.view`. Zones cached per camera (10 s TTL, invalidated by zone CRUD). `GET /live-state[?camera_id]` (scoped; token route) adds `last_plate` and `last_object` (latest finished visit with its best-frame `evidence_url` and zone_ids) from the event store. **Deviation from the plan:** no separate KAI-C frame capture at track confirmation; the last-object image is the existing best-frame evidence, so there is no extra decode or storage. Live: counts on all 5 fakecams (e.g. cam 3: 4 cars + 2 trucks), a cam-1 token socket got 9 change-only `live_state` events in 30 s including track ends, and the token's GET saw only cam 1. Server suite 1704 passed, baseline 4 failed. |
| HA-111 | done | SRB-ha-integration | (this commit) | **Bus:** per-process `epoch`, a `seq` counter, and a 5-min / 20k-event replay ring that records **even with no subscriber** (a disconnected client is who needs replay). Subscribers record `start_seq` under the lock; v2 subscribers receive `(seq, event)`, v1 the bare dict. New client `types` filter next to the entitlement boundaries. `replay(sub, since)` → complete, or incomplete (gap, pruned, future). **Socket `v=2`:** subscribe first, then send history, then drain the queue (lossless). Frames: `subscribed` {epoch, seq, resumed}; then either the replay (since+same epoch) or a `state_snapshot` {cameras: live state + `online`, `resync`}; events as `{v, seq, ...event}`; `heartbeat` after 25 s idle; `lagged` with the drop count. v1 unchanged, pinned by tests (bare dict, same hello). **App alerts** push the STORED values (`apply_alert(..., out=)`): the row id, the normalised severity (an "info" alert used to be pushed as "info" but stored as "low"), source {kind, name} and correlation_id. `scripts/ha-dev/ws_load.py`: N concurrent v2 clients. Live via nginx: 20 clients × 34 s all got the snapshot, 153–156 frames each, 0 out-of-order, 0 lagged, last-seq spread 3; a resume after 5 s replayed from seq+1; a wrong epoch got `state_snapshot` with `resync: true`. Server suite green (baseline 4). |
| HA-112 | done | SRB-ha-integration | (this commit) | `services/media_signing.py`: token `m1.<payload>.<HMAC-SHA256>`; payload = kind, resource, expiry (60 s..7 d, default 24 h), key id, signer user, API token id. Keys in site setting `media_signing_keys`; `POST /media/keys/rotate` (superuser) keeps the previous key (rotate twice = revoke all). `routers/media.py`: `POST /media/sign` (kinds `event` {evidence|scene|plate|plate_frame}, `alert_image` {image key}, `clip` {camera, start, ≤3600 s}; needs the kind's permission (recordings.view / alerts.view) and the camera, and for clips the token allow-list; audited `media.sign`) and `GET /media/s/{token}` (no login; **the signer is re-checked on every fetch**: user active, API token live, camera still visible; audited `media.fetch` at most once per URL per 10 min; `release(db)` before streaming). The device firewall opens ONLY `/api/v1/media/s/`. Clip streaming factored out of `export_clip` as `recordings.stream_playback_clip` (shared; `inline` for signed clips). Token route `POST /media/sign`. Live via nginx with a cam-1 token: an anonymous fetch returned the event JPEG (15 KB), a signed 5 s clip streamed as MP4 (1.8 MB, ftyp), a clip on cam 2 → 403, after the token was revoked → 403, tampered → 403. Server suite 1723 passed, baseline 4 failed. |
| HA-113 | done | SRB-ha-integration | (this commit) | `services/media_ready.py`. Uses the HA-007 result: the open segment plays, so a clip window is ready `MEDIA_READY_LAG_S` (2 s) after it ends; no waiting for the segment to close. Clip window = [start − `MEDIA_CLIP_PRE_S` (5), end + `MEDIA_CLIP_POST_S` (5)], capped at 120 s, keeping the END (where a long visit's interesting part is). **Visits:** a new ingest (never a duplicate) schedules one `media_ready` {source: event, id, label, images: [evidence|scene|plate|plate_frame present], clip: {start, duration_s}, zone_ids} for when the clip plays. **Alerts:** images are stored with the alert, so the `app_alert` push itself now names them (`images`); if the alert has a camera, a `media_ready` {source: alert} follows for the clip around `observed_at` (else `fired_at`). Bus type `media_ready` (token scope `recordings.view`). Clients sign what they want via HA-112. Live: a real car visit produced `media_ready` (images evidence+scene, 39.8 s clip); both signed and fetched anonymously (the clip was a full 18.5 MB MP4). Server suite green (baseline 4). |
| HA-118 | done | SRB-ha-integration | (this commit) | `services/site_mode.py`: `disarmed | armed_home | armed_away` in site setting `site_mode` {mode, changed_at, changed_by}; a site that never set one is `armed_away` (today's behaviour). **Behaviour, v1 (my call, the design only says "arming changes alert delivery"):** disarmed skips alarm ACTIONS (Twilio call/SMS, webhook/hooter) in `dispatch_alarm_actions`; alerts are still stored, shown and pushed; `force` (the test button) still runs; both armed modes dispatch as configured and differ only for HA automations. `GET /site-mode` (settings.view) and `PUT /site-mode` (settings.manage; audited from/to/reason; pushes `site_mode`). Bus: `site_wide: True` events pass camera entitlement and the camera filter (set only by `publish_site_mode`; carries no camera data), so camera-scoped tokens get it; token scope `settings.view`. v2 `state_snapshot` gains `site_mode` (null if the socket isn't entitled). Tokens: `settings.manage` added to ALLOWED_SCOPES; it reaches only `PUT /site-mode` (routes are opt-in; a test proves `/audit-logs` still 403s). SPA: "Site mode" card on the Alarms page (visible to all; buttons for admins) explaining what disarmed means, plus the new token scope label (en/fr). Live: a camera-1 token with settings.manage disarmed the site; its own v2 socket (types=site_mode) got `site_mode` disarmed; re-armed to armed_away; both changes audited with actor. Server suite 1736 passed, baseline 4 failed. |
| HA-114 | done | SRB-ha-integration | (this commit) | `services/entity_descriptors.py`: `Descriptor` (key, platform, name, device {site|camera|zone|app}, required_scope, origin, presentation, options, event_types, typed command, descriptor_version). **Core catalogue:** site (unacked alerts: problem/count/highest severity, ack-all button, storage, cpu/memory off), camera (online, motion, occupancy + count + active count per label (assignment labels, else person/car/truck/dog/cat), recording problem, last plate, last object image, detection switch, manual-event button, detection and alert event entities, fps/inference/bitrate diagnostics off; recording switch ONLY with the pause flag, off by default), zone (occupancy/count per zone label + detection events), PTZ buttons + preset select only for cameras whose probed `CameraCapability.supported_areas.ptz` is true (`services/ptz_presets_cache.py`, 5 min). **App catalogue** from manifest `entities:` (skips unknown platforms, bad keys, controls naming undeclared actions; `per_camera` → one per camera in the app's roster). Visibility = the descriptor's scope (token-aware) AND the camera. **States resolved server-side** (`resolve_states`: live state, camera status, event store, recording state, pause state, alerts, monitor sample, cached stats, app `/state` via `eval_path`: dot path, list index, `[field=value]`, `{camera}`). `services/entity_state_publisher.py`: one site-wide loop (2 s; apps 5 s; stats + PTZ 30 s) publishing `entity_state` only on change and `descriptors_changed` when the ETag moves. Bus: `v2_only` (v1 never sees them) and a per-event `required_scope` enforced by the v2 socket (`_v2_may_send`, replay too); snapshot gains `entity_states`. Detection (camera + zone) and alert event entities pushed from the track consumer / alerts inbox. `routers/entities.py`: `GET /entities` (ETag, 304), `GET /entities/states`, `POST /entities/{key}/command` (only descriptors the caller can see → 404 otherwise; camera-changing controls re-check owner rights like PUT /cameras; app actions via `invoke_app_action` with the camera handle and only declared params; audited `entity.command`). Tokens: `apps.view` scope, three routes. **SDK 0.7.0:** `Entity` dataclass, `AppManifest.entities`, `to_dict`, `check_manifest` rules, export, regenerated reference docs (`publish-sdk.yml` publishes only on `sdk-v*` tags, so the bump publishes nothing). Facade needs no change (`entities=` passes through `**manifest_kwargs`). **abandoned-object** declares 5 entities (site counts, per-camera unattended, per-camera acknowledge button); its manifest validates clean. Live: admin 147 descriptors (304 on the same ETag); a cam-1 token 23 (all cam 1) and 22 snapshot states; its detection-off command worked and was pushed as `entity_state`; detection events arrived; cam 2 → 404. Server suite 1746 passed (baseline 4); SDK 637 passed (baseline 4); example 45 passed. |
| HA-116 | done | SRB-ha-integration | (this commit) | **Order change:** done before HA-115 so the 1.0.0 freeze includes it. `GET /search` (`routers/search.py`): `q, type=all|events|alerts, camera_id, label, zone (id or name, case-insensitive; 404 unknown, 409 ambiguous across cameras), plate (normalised), source, severity, from, to, limit ≤100`; newest first; each result `{kind: event|alert, at, camera_id, ...}` (events are the `/events` serialisation incl. `evidence_url` and `zone_ids`). `q` = label/plate ILIKE on events, title/description/source on alerts; `semantic: false` until HA-502. Zone filter = four LIKE shapes on the JSON text (`[z]`, `[z,…`, `…, z]`, `…, z,…`): dialect-neutral, proven on SQLite and on the live Postgres, and never a prefix match (zone 1 ≠ 11). Label/plate/zone filters drop alerts rather than matching them loosely. Scope: events need `recordings.view`, alerts `alerts.view`, both only on visible cameras; a caller with neither gets an empty list; a token route (`cameras.view`). Live: zone=1 found the HA-109 visit, label, free text (the manual doorbell event) and alert queries behaved, and a cam-2 token saw only cam 2 events. Server suite 1752 passed (baseline 4). |
| HA-117 | done (mDNS unverified on Linux) | SRB-ha-integration | (this commit) | `docs/HOME_ASSISTANT.md` (linked from docs/README): what HA uses (API/WS/signed media via nginx; WHEP via `/webrtc`; ICE on 8189; optional RTSPS; optional mDNS), the settings, why, and the token scopes to pick. **Found:** RTSPS was hard-bound to 127.0.0.1 and `MEDIAMTX_EXTERNAL_RTSPS_URL` / `CORS_ORIGINS` were NOT settable from `.env` (compose hard-coded the first and never passed the second). Now `RTSPS_BIND_HOST` (default 127.0.0.1), `MEDIAMTX_EXTERNAL_RTSPS_URL` and `CORS_ORIGINS` pass through with defaults equal to today's values (compose config verified; core recreated healthy; CORS preflight for localhost:5173 still allowed). `.env.example` documents them. **mDNS:** `scripts/mdns-announcer/` (python-zeroconf 0.151, non-root image) announces `_opennvr._tcp` with public facts only (txtvers, path, https, port, version from /health); compose service `opennvr-mdns`, profile `mdns`, `network_mode: host`, **Linux only** (Docker Desktop keeps multicast in its VM). Unit tests for the TXT builder and address parsing; the image builds, refuses to run without `OPENNVR_HOST_IP` (exit 2), and registers then unregisters cleanly on SIGTERM (exit 0) in a container. **Not verified end to end:** real LAN discovery needs a Linux host; HA-side zeroconf is covered by phcc tests in M2. |
| HA-115 | done | SRB-ha-integration | (this commit) | **Contract frozen at 1.0.0**, last in M1 (after 116/117) so it includes every M1 surface. `server/contract/contract.json`: 32 REST endpoints (method, path, token scope, response schema), websocket v2 (control frames subscribed/state_snapshot/heartbeat/lagged, the event frame, 9 event types with their fields), descriptor (required/optional fields, platforms, command types, core controls) and 29 payload schemas in a small JSON-Schema subset validated by `core/contract_schema.py` (no new dependency). `server/contract/fixtures/`: 13 example payloads for pyopennvr (M2). `core/contract.py`: `CONTRACT_VERSION = "1.0.0"`, `FEATURES` lists every M1 capability. `tests/test_contract.py` holds the code to the file: version equality, every endpoint served with the SAME token scope, ws event types actually published, descriptor fields = the dataclass, controls handled, fixtures conform, and REAL route/websocket responses conform (system info, cameras, zones, live state, site mode, entities, states, search, events, ws ticket, v2 subscribed + snapshot, a manual event). `scripts/contract_check.py --base <ref>`: breaking (removed/narrowed/retyped/new enum values) needs MAJOR, additive needs MINOR, never backwards, code/file versions must match; rule tests included. CI server job runs it against origin/main (shallow fetch first) and now also runs the mDNS announcer tests. Server suite 1771 passed (baseline 4). |
| M1 review | done | SRB-ha-integration | folded into the issue commits | Independent security/correctness review of e59c8b4..M1 (no critical findings; no signed-URL forgery or token escalation found). 14 findings, all fixed in the commit of the issue they belong to, each with a test: (1) open token sockets outlived a revoke or a scope change: re-checked every 30 s, closed with 4401 [HA-111]; (2) `/live-state` gave plate text to `cameras.view`-only tokens: now needs `recordings.view` [HA-110]; (3) the entity recording-pause command took any `resume_after_s`, and a huge one stopped recording before failing, leaving an unlisted pause: validated, and `pause()` validates before touching MediaMTX [HA-114, HA-108]; (4) app actions on a camera 500'd for admin-owned tokens (scope None): fixed in `invoke_app_action` [HA-114]; (5) `/entities/states` re-resolved everything each call and blocked the loop: stateful keys only, resolution off-loop [HA-114]; (6) random `onvr_` bearers cost a DB query each in the firewall: in-memory table of live digests, one load per 15 s, reloaded on mint/revoke [HA-101]; (7) default-install costs: `tracks` no longer kept in the replay ring [HA-111], overlay-off stops the consumer again [HA-110], the entity publisher is idle unless a v2 socket or a recent REST client uses it, `last_*` lookups use the (camera, started_at) index with a 7-day bound, alert counts are one GROUP BY [HA-114]; (8) signed-media fetch now re-checks the kind's permission [HA-112]; (9) recording-pause restore runs even when auto-provisioning is off or fails [HA-108]; (10) site alert counts hidden from camera-restricted callers [HA-114]; (11) zone refresh off the event loop [HA-110], stats refresh one short session per camera, PTZ/stats refresh beside the tick [HA-114]; (12) zone names exact and scoped to visible cameras, alert camera filter in SQL [HA-116]; (13) entity manual-event label/note and switch values validated [HA-114]; (14) the contract check fails on an unresolvable base ref, and `features` is part of the contract [HA-115]. |
| HA-201 | done | SRB-ha-integration | (this commit) | `pyopennvr` 0.1.0 (`integrations/home-assistant/pyopennvr`). `OpenNVRClient` on an injected aiohttp session: Bearer token, optional `X-Correlation-Id`; errors 401/403 → `OpenNVRAuthError(status)`, 404 → `OpenNVRNotFoundError`, other 4xx → `OpenNVRRequestError(status, detail)`, 5xx/transport → `OpenNVRConnectionError`; 304 → None (entities ETag). Absolute URLs the server returns (webrtc, signed media) are **re-rooted onto the URL HA was configured with**, because core builds them from its own idea of its host. Query params drop None and send booleans as `true/false`; `from_` → `from`. Entity catalogue skips platforms it doesn't know (kept in `skipped` for diagnostics). `check_contract`: major must be 1 (`OpenNVRContractError`). `EventStream`: a ticket per connection, v2, resumes with `since`+`epoch` from the last EVENT seq (never a heartbeat's, which can run ahead of a queued event), resets on a new epoch, `receive_timeout` 90 s as the dead-connection detector, backoff 1..60 s with jitter reset on `subscribed`, 4401 or a refused ticket → `auth_failed` and stop. `Whep`: offer with the stream token, `Location` rebuilt under `/webrtc`, trickle PATCH, DELETE. Tests (27) replay the server's contract fixtures (a test fails if the copy drifts) against a real in-process aiohttp server: aioresponses 0.7.9 is broken on aiohttp 3.14, and HA's pytest plugin is disabled for the library suite. |
| HA-202 | done | SRB-ha-integration | (this commit) | **Integration skeleton** (`hass-opennvr/custom_components/opennvr`): `ConfigEntry[OpenNVRData]` runtime_data; `OpenNVRCoordinator` (`config_entry=`, 30 s REST refresh of system info, cameras, catalogue via ETag, states, site mode; `_async_setup` refuses another contract major or a different `site_id` at the URL with ConfigEntryError; 401/403 → reauth). Push: the coordinator owns pyopennvr's `EventStream` (background task, stopped on unload), asks only for the frame types HA uses (**the `tracks` firehose filled the socket queue: 89 frames in 12 s live**), updates one key per `entity_state` and wakes only that key's listeners (`async_add_key_listener`), merges `state_snapshot`, re-reads on `descriptors_changed`/`lagged`, forwards other frames on a dispatcher signal; `auth_failed` on the socket starts reauth. Config flow: user (URL normalised, token must be `onvr_`, SSL error told apart), cameras (all selected = none stored = future cameras included), zeroconf (URL dedup; **never rewrites a configured entry's URL**, since an mDNS announcement is unauthenticated), reauth and reconfigure (both refuse a different `site_id`), options (cameras, notification link TTL, reload). Devices: site, camera (via site), zone (via camera), app; unique ids `<site_id>:<key>`. Diagnostics redact token, URLs, usernames, plates. `brand/icon.png` (+@2x), `quality_scale.yaml`, translations incl. exceptions; hassfest clean. **Core, additive (contract 1.1.0, feature `caller_info`):** `/system/info` reports the caller (token name, effective scopes = token ∩ owner, cameras, expiry; never secrets), because the flow must name missing scopes and HA-206 warns before expiry; pyopennvr gains `SystemInfo.caller/scopes/token_expires_at`, `OpenNVRSSLError`, `EntityCatalog.from_dict`. **Core fix found live:** after idling, the entity publisher's first pass published all ~126 states at once into a 100-slot subscriber queue (`lagged`, 31 dropped). Now a cold pass primes silently, a socket warms the cache before its snapshot (so the snapshot is complete), and a resume across an idle spell is answered with a resync snapshot (states changed then were never published, so the ring can't replay them). Live after a 65 s idle: snapshot 126 states, no `lagged`. Tests: HA 44, pyopennvr 28, server 1789 passed (baseline 4 failed). |
| HA-203 | done | SRB-ha-integration | (this commit) | `camera.py`: one entity per shown camera (added as cameras appear; none without `live.view`, where it could only fail). **Native WebRTC** (`async_handle_async_webrtc_offer` → pyopennvr `Whep.offer` → `WebRTCAnswer`; `WebRTCError("webrtc_offer_failed")` with "not streaming" on 404); trickle candidates PATCHed, and **candidates that arrive before the WHEP answer are held and sent after it** (the browser trickles as soon as it has a session id); `close_webrtc_session` DELETEs; open sessions closed on removal. Snapshot from `/snapshot` (None on error → HA's own error). Motion detection ↔ detection flag (pushed `camera.N.detection` wins over the camera row); `ON_OFF` **only when `recording_pause_enabled`**; unavailable when `camera.N.online` is pushed false. `stream_source`: RTSPS with the stream JWT as the password (MediaMTX JWT auth), **None while OpenNVR publishes RTSPS only on loopback** (the default), so HA never tries a URL that can't work. Commands carry HA's context id as `X-Correlation-Id`; 401/403 → `not_permitted`, other failures → `command_failed` (translated). **Live** (real SDP offer from aiortc, token with live.view, through nginx): answer 1951 B, session URL rewritten under `/webrtc`, trickle PATCH ok, **video track received**, DELETE 200. Test image gains PyTurboJPEG (camera component requirement). HA suite 61 passed; hassfest clean. |
| HA-204 | done | SRB-ha-integration | (this commit) | `descriptor.py` + sensor, binary_sensor, switch, select, button, number, event, image. One generic base: name from the server (an older integration can't know newer descriptors' translation keys), icon, entity category, enabled-by-default; device class / state class / unit mapped with **unknown values ignored, never an error**; state and attributes are the server's; each entity listens to its own key only; commands go through `POST /entities/{key}/command` with HA's context id as correlation id (401/403 → `not_permitted`) and show their effect at once until the next push confirms. Enum sensors report unknown for a value the descriptor didn't announce; select options are re-read from the current catalogue (new PTZ presets); number `{min,max,step}` from `options`; event entities fire pushed `{"event"}` updates and **drop types the descriptor didn't announce** (HA requires declared types; the catalogue lists the camera's configured labels, as the counts do); image fetches the event's evidence through a 120 s signed URL (so a self-signed certificate and `recordings.view` suffice) and caches per event. **Lifecycle:** entities are added as descriptors appear; after every refresh `async_prune` removes entities whose descriptor or camera is gone (deleted zone, deselected camera) and then devices left without entities (**not** on a failed refresh: data is kept, entities go unavailable); `async_remove_config_entry_device` lets the user delete a device OpenNVR no longer has. Coordinator indexes descriptors by key (availability checks were O(n²)). Found: HA's `NumberEntity` has its own `value` property; the base's accessor is `resolved`. HA suite 71 passed; hassfest clean. |
| HA-205 | done | SRB-ha-integration | (this commit) | `alarm_control_panel.py`: the site mode as an alarm panel (created when the server has `site_mode` and the token may read it): state from the coordinator (REST + pushed `site_mode` frames), `changed_by` from the server, a mode a newer server adds reads as unknown; arm home / arm away / disarm → `PUT /site-mode` (reason "Home Assistant", HA context id as correlation id), **features only when the token holds `settings.manage`**, otherwise read-only; no code (HA users add one in automations). `update.py`: server version; `latest_version` from `/system/info` only when the operator opted in (`UPDATE_CHECK`), else equal to installed so it never claims an update; release notes link; no install from HA. Both kept by the prune. Translations for their names. HA suite 77 passed; hassfest clean. |
| HA-206 | done | SRB-ha-integration | (this commit) | `issues.py` + `repairs.py`: the design §7.9 ids, per entry (`<id>_<entry_id>`), each **cleared as soon as its condition is gone**. `token_revoked` (REST 401/403, or the socket's 4401; fix flow → reauth), `token_expiring` (< 7 days **by the server's clock**; fix flow → reauth), `firewall_blocked` (the server's new **`X-OpenNVR-Error: token_address`**: a new token with the same settings wouldn't help, so no reauth, the entry retries), `server_too_old` (no `/system/info`, or an older major) / `integration_too_old` (newer major), `webrtc_hosts_unset` (the server's `network.webrtc_ice_hosts` false: the same condition core itself logs as "live WebRTC will fail"), `rtsp_not_exposed` (**only when HA actually asks for an RTSP stream**, so the secure default doesn't nag), `clock_skew` (> 60 s against the `server_time` read in the same refresh), `ssl_unverified`. **Core, additive, same unreleased contract 1.1.0 (feature `network_info`):** `/system/info` `server_time` and `network {webrtc_ice_hosts, rtsps_exposed}` (booleans only), and an `errors` section naming `X-OpenNVR-Error`. **Pre-existing bug found live:** main.py's HTTPException handler rebuilt every error response WITHOUT the exception's headers, so no 401 ever carried `WWW-Authenticate` and the new header never left the server; now kept (`core/http_errors.py`, tested; live: 401 `www-authenticate: Bearer`, 403 `x-opennvr-error: token_address`). **Finding for the owner:** on this Docker Desktop stack `webrtc_ice_hosts` is false (nginx's `$server_addr` is a container address, never learnable, and `MEDIAMTX_WEBRTC_HOSTS` is unset), so remote live WebRTC likely falls back to HLS here; the repair will say so. pyopennvr: `OpenNVRAuthError.code`, `SystemInfo.server_time/network`. Tests: HA 89, pyopennvr 29, server 1792 passed (baseline 4 failed). |
| HA-207 | done | SRB-ha-integration | (this commit) | `scripts/ha-dev/e2e-ha.ps1` + `e2e_ha.py`: a **fresh** HA 2026.9.2 (`run-ha.ps1`, default bridge, port 8124, config `D:\myData\ha-dev-e2e` recreated) against the running stack through the host's nginx. **Not under `tests/e2e`**: that runner lives on `opennvr_internal` by design, and a dev HA must stay outside it (S7) to meet the device firewall like a LAN install. Checks: (1) onboarding API; (2) config flow with a freshly minted token (URL `https://host.docker.internal`, verify off, all cameras); (3) entities: 5 cameras, 75 sensors, 46 binary sensors, 10 event, 5 image, 5 switches, alarm panel, update, all available; (4) occupancy seen on AND off, pushed (fakecam `Road No 2`); (5) restart: entities back 8.5 s after HA's API answers, of which **the integration's own setup is 0.5 s** (HA's `integration/setup_info`), the rest HA's bootstrap; (6) a detection switch toggled from HA is in OpenNVR's audit log under HA's context id (`X-Correlation-Id`), then restored; (7) revoking the token raises `token_revoked` (socket 4401 within the 30 s recheck). **8/8 passed** (three runs). PTZ preset audit (plan) swapped for the detection switch: no camera here has PTZ. **Found by it, fixed here:** HA 2026.9 deprecates `DeviceInfo.via_device` (stops working in 2027.8; warned per entity): the integration now registers the device tree itself, parents first, linking by `via_device_id` (`entity.async_register_devices`, before platforms and on every refresh), and a test fails on any deprecation warning; the coordinator's refresh reads cameras/catalogue/states/site mode in parallel and reuses setup's `/system/info` (first refresh 1.6 s → about 0.5 s). HA-dev image pulled (3.4 GB on C:); 20 GB of dangling build cache pruned first. |
| M2 exit | done (push pending owner) | SRB-ha-integration | — | pyopennvr 0.1.0 and integration 0.1.0 complete in `integrations/home-assistant/`; contract 1.1.0 (caller_info, network_info; additive, unreleased). Suites: server 1792 passed (baseline 4 failed), pyopennvr 29, HA 89, hassfest clean, e2e-ha 8/8. Not installable by end users until pyopennvr is on PyPI (HA-306). Nothing pushed since e59c8b4; waiting for the owner. |
| HA-301 | done | SRB-ha-integration | (this commit) | `services.py` + `services.yaml`, registered once in `async_setup` (action-setup): `ptz` (move up/down/left/right, zoom in/out, stop, preset by name or token; speed), `create_event` (→ `event_id`), `end_event`, `export_recording` (a signed clip URL valid for the entry's notification-link lifetime, ≤ 1 h clips; `with_hash` downloads it once for sha256 + size), `protect_recording`, `ack_alerts` (ids, or source/severity filter, or all; → count; ids and filter exclusive, as the server insists), `search_events` (→ results, events with a signed `thumbnail_url` in place of the auth-only `evidence_url`). The site is found from the targeted OpenNVR camera entity, an explicit config entry, or the only loaded one; a non-OpenNVR camera, an unloaded site or two sites without a choice are validation errors (translated). Every call sends HA's context id as `X-Correlation-Id`. pyopennvr: `ack_alerts(source=, severity=)`. **Deferred:** `summarize_period` needs a server endpoint that doesn't exist (M5, HA-502); `protect_recording` by camera + range (the server protects around events only); relay URLs for off-LAN links come with HA-303. HA suite 98, pyopennvr 30, hassfest clean. |
| HA-208 | done | SRB-ha-integration | (this commit) | **M2 review** (independent agent over 9a950e4..HA-207; 14 findings, 2 high; every one checked against the code, all fixed with tests). (1) HIGH: turning a camera off (or `GET /cameras/` defaulting to `active_only=true, limit=100`) made it vanish from the catalogue and the prune deleted its entities and device, so HA could not turn it back on: pyopennvr lists all cameras, turned-off ones included, across pages; core's catalogue keeps turned-off cameras; the camera entity stays available (off). (2) HIGH: 4401 also means "what you may see changed", and treating it as a dead token ended push for good while REST cleared the repair: the stream now reconnects, and only a refused TICKET means `auth_failed`; a ticket refused for the address (`token_address`) is retried, never reauth. (3) A value HA rejects (e.g. an ISO string for a timestamp sensor) raised out of the socket consumer and killed push: sensors coerce by kind (timestamp/date parsed, numerics or unknown, strings ≤ 255), key listeners and the stream's consumer call are guarded. (4) A REST refresh replaced state pushed while it was in flight (a short motion pulse undone): pushes, snapshots, site mode and optimistic command states during a refresh are merged over its result. (5) Descriptor presentation (event types, enum options, names, number ranges) was fixed at creation: re-applied whenever the catalogue changes. (6) A descriptor briefly absent (PTZ presets after an OpenNVR restart) was pruned and came back as a NEW entity: removal waits for 3 refreshes (a deselected camera still goes at once). (7) WHEP: a session closed while its offer waited leaked on MediaMTX, and a timeout never answered the browser: closed-early sessions are DELETEd on arrival, every failure sends `WebRTCError`, pyopennvr maps `TimeoutError`. (8) The RTSPS source's 60-min token went stale inside HA's long-lived stream: re-signed every 50 min via `stream.update_source`. (9) Core: a resume from before the entity cache was re-primed (another client woke the publisher) was treated as lossless: the publisher records the priming seq and such a resume resyncs. (10) Config flow told an address-refused token it lacked `settings.view`: own error. (11) Hand-written entities were kept forever by the prune even when their platform no longer creates them: kept only when it does. (12) Zeroconf confirm now defaults to verifying TLS (the address came from an unauthenticated announcement). (13) A WHEP `Location` on another origin would have received the stream token: refused. (14) `e2e-ha.ps1` only deletes a config dir carrying its own marker. Plus: entry-scoped tasks for refreshes/closes. Tests: server 1794 passed (baseline 4), pyopennvr 34, HA 107, hassfest clean, e2e-ha 8/8. |
