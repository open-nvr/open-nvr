# Copyright (c) 2026 OpenNVR
# SPDX-License-Identifier: AGPL-3.0-or-later
#
# camera-agent config consumed by docker-compose.camera-agent.yml.
#
# Variables of the form ${VAR} are substituted by the
# camera-agent-config-init container at compose-up time using
# envsubst. Only the variables explicitly listed in the init
# container's envsubst invocation are replaced — everything else
# (e.g. the system_prompt multi-line block) is left untouched.
#
# DO NOT edit the substituted values directly. To change a secret,
# update .env and re-run ``docker compose up`` so the init container
# re-templates the file.

# KAI-C inside opennvr-core. Adapter calls flow here for audit.
kaic_url: http://opennvr-core:8100

# Tier-0 best frame (detect-pipeline). When reachable, describe/VLM calls run
# on Tier-0's sharpest retained frame for the track instead of an arbitrary
# live grab — better answers, cheaper. Unreachable/404 falls back to the live
# grab automatically, so this is safe to leave on even if Tier-0 is disabled.
bestframe_base_url: http://detect-pipeline:9109
kaic_api_key: ${INTERNAL_API_KEY}

# Streaming voice path. Whisper and Piper use their published
# contract-v1 adapter images; Ollama is called directly on its native
# HTTP API until a standalone ollama-adapter image is published.
whisper_url: http://whisper-adapter:9003
whisper_token: ${INTERNAL_API_KEY}
ollama_url: http://ollama:11434
ollama_token:
piper_url: http://piper-adapter:9001
piper_token: ${INTERNAL_API_KEY}

# The agent has no persona name — it presents as "the OpenNVR camera agent"
# (chat label "agent"). This value is only used for event metadata and, if you
# turn it on, the OPTIONAL wake word ("Hey <agent_name>"). The voice is a
# SEPARATE setting — "neutral" pairs an androgynous Piper voice.
agent_name: Camera Agent
voice_gender: neutral
#   wake_words: ["camera"]     # extra exact wake words, if you enable the wake word

# Wake word — OPTIONAL. The demo uses click-to-talk (tap Talk → it listens and
# answers every utterance, auto-stops on a quiet spell), so it never hits this
# gate. It only affects always-on callers of /converse that don't pass ?wake=0:
# when true they answer only when addressed by name ("Hey <agent_name>, …"), so
# ambient speech and the agent's own echoed reply don't trigger a turn. Exact
# match on the name (+ spellings), a tight fuzzy net (wake_fuzzy 0.85) for drift,
# and an address word ("hey/ok") required — the "Hey Siri" model.
wake_word_required: true
#   wake_require_prefix: true   # false = the bare word (no "hey") also wakes it
#   wake_fuzzy: 0.85            # 1.0 = exact only; < 0.85 = more forgiving

# Vision tools — routed through KAI-C by the registered adapter NAME.
# The standard stack stack registers ONLY the YOLOv8 object detector, under the
# name ``default`` — so detect_objects must target ``default``.
# BLIP (caption) and InsightFace (recognition) are NOT registered in the
# standard stack KAI-C, so describe_camera / recognize_faces will report they're
# unavailable until those adapters are registered. (They still point at
# their intended names so they light up automatically once registered.)
detection_adapter: default
recognition_adapter: insightface
caption_adapter: ${CAPTION_ADAPTER}   # moondream (default) — auto-registered by caption-adapter-register

# Tools advertised to the LLM. standard stack only registers the YOLOv8 object
# detector, so the demo exposes just the two tools that actually work:
# detect_objects (counts people/cars/…) and describe_camera (which falls
# back to the detector when no caption adapter is present). This keeps the
# prompt short (faster CPU prefill) and stops the model calling face
# recognition / footage search whose adapters aren't deployed. Add names
# here once you register BLIP / InsightFace / the footage index.
enabled_tools:
  - detect_objects
  - describe_camera

# LLM tuning. qwen2.5:1.5b is the docker-compose.camera-agent.yml default —
# ~1 GB of RAM, snappy on CPU, Apache-2.0, and NON-thinking (the right choice on
# CPU: a thinking model can burn its whole token budget on hidden reasoning). For
# more headroom set OLLAMA_MODEL=qwen2.5:3b in your .env — that single var drives
# BOTH the model pull and this config; qwen2.5:0.5b is the low-RAM floor.
# Whichever model you pick, it MUST support tool calling — see
# https://ollama.com/blog/tool-support.
llm_model: ${OLLAMA_MODEL}
# Pre-set so that IF you switch OLLAMA_MODEL to a "thinking" model (e.g. a qwen3)
# it stays snappy and non-verbose; a harmless no-op for the non-thinking qwen2.5
# default.
llm_think: false
llm_temperature: 0.4
# Spoken replies are 1-2 short sentences (see system_prompt). 256 let the
# model ramble for ~20s of CPU generation; 128 is plenty and noticeably
# faster per turn.
llm_max_tokens: 128

frame_cache_ttl_seconds: 2.0
event_ring_size: 256

# NATS broadcast — picks up inference events fanned out by KAI-C so
# the recent_events tool can answer "did anyone walk past in the
# last 30 minutes?". The token matches the NATS --auth value in the
# standard stack compose (which uses INTERNAL_API_KEY).
nats_inference_url: nats://nats:4222
nats_inference_token: ${INTERNAL_API_KEY}

# Load cameras from OpenNVR. OpenNVR returns internal MediaMTX frame-source
# URLs, so camera-agent reuses the stream OpenNVR already manages instead of
# storing camera RTSP credentials in this example config.
opennvr_cameras_url: http://opennvr-core:8000/api/v1/internal/camera-agent/cameras
opennvr_api_key: ${INTERNAL_API_KEY}

# Browser-facing OpenNVR origin — feeds opennvr_ui_url (deep-links to AI
# Adapters / App Catalog when a skill is greyed out). Set to the HOST origin
# the operator's browser reaches OpenNVR at — the compose stack publishes
# opennvr-core on 127.0.0.1:8000, so this defaults to http://localhost:8000.
# Override via OPENNVR_UI_URL in .env for LAN/remote installs
# (e.g. https://nvr.example). Deep-links open in your browser, which can't
# resolve compose service names.
opennvr_base_url: ${OPENNVR_UI_URL}

# App door (read-only): the agent fetches installed apps from this origin
# FROM INSIDE the container, so it must be the compose-internal service name
# — same origin as opennvr_cameras_url above, NOT the browser-facing URL.
# Without this explicit override, load_config would derive it from
# opennvr_base_url (localhost:8000 = the agent container itself) and the
# app door would silently never work under compose.
opennvr_api_url: http://opennvr-core:8000

# Require an OpenNVR sign-in before anyone can talk to the agent. Every
# non-page endpoint then needs an OpenNVR-issued bearer token, validated
# against opennvr_api_url and mapped to viewer/operator/admin by role — the
# agent mints no credentials of its own. The /demo page shows a branded
# login. Set to "none" only for a throwaway open-loopback demo.
auth_mode: opennvr

# TLS: self-signed pair generated by camera-agent-certs-init and mounted by
# the compose overlay. Serving https is what lets the demo mic work from
# LAN devices (getUserMedia needs a secure origin).
tls_certfile: /certs/server.crt
tls_keyfile: /certs/server.key

host: ${CAMERA_AGENT_LISTEN_HOST}
port: 9100

# Optional manual fallback. Leave empty in Docker so camera-agent uses the
# OpenNVR-configured cameras from opennvr_cameras_url above.
cameras: []

system_prompt: |
  You are a concise voice assistant for a home security camera system.
  You have NO knowledge of what any camera currently shows — the ONLY
  way to know is to call a tool.

  RULES:
  - For ANY question about what a camera sees, what is happening, who or
    what is present, or how many of something, you MUST call detect_objects
    or describe_camera BEFORE answering.
  - NEVER invent, guess, or describe what is on a camera from imagination.
    If you have not called a tool this turn, you do not know.
  - Base your answer ONLY on the tool result, in 1-2 short spoken sentences.
  - If a tool says a camera cannot be reached, tell the user that camera
    appears to be offline.
